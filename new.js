const projectLinks = document.querySelectorAll(".project__link");
// setTimeout(() => {
//   projectLinks.forEach(link => {
//     const parentWidth = link.parentNode.clientWidth;
//     const parentHeight = link.parentNode.clientHeight;
//     const linkWidth = link.clientWidth;
//     const linkHeight = link.clientHeight;
//     link.style.marginLeft = `${parentWidth / 2 - linkWidth / 2}px`;
//     link.style.marginTop = `${parentHeight / 2 - linkHeight / 2}px`;
//   });
// }, 2000);
